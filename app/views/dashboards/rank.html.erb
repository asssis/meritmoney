
<%= render 'nav', nav: 'rank' %>


<div class="card">
  <div class="card-body">
    <h5 class="card-title">Rank</h5>
    <table class="table">
      <thead>
        <tr>
          <th>USER</th>
          <th>TOTAL</th>
          <th>VISUALIZAR</th>
        </tr>
      </thead>
      <tbody>
        <% @ranks.each do |item| %>
          <tr>
            <td><%= item[:email_name] %></td>
            <td><%= item[:total] %> $</td>
            <td>
            <button type="button" class="btn btn-primary" onclick="consultar_detalhes(<%=item[:user_id]%>)">
                Details
            </button>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= render 'pagination' %>
  </div>
</div>



<%= render 'detalhes' %>

